import { FilterRacePipe } from './filter-race.pipe';

describe('FilterRacePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterRacePipe();
    expect(pipe).toBeTruthy();
  });
});
